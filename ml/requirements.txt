# ML SIDECAR RUNTIME DEPENDENCIES — `pip install -r requirements.txt` INTO THE VENV.
fastapi>=0.115
uvicorn[standard]>=0.30
onnxruntime>=1.19
rapidocr>=3.0
opencv-python-headless>=4.10
numpy>=1.26
pillow>=10.0
python-multipart>=0.0.9
pyclipper>=1.3
# OPTIONAL (RECOMMENDED): HIGH-QUALITY LaMa INPAINTING — INSTALL SEPARATELY (PULLS TORCH, ~200MB+):
#   pip install torch
# PLUS THE WEIGHTS: python scripts/download_models.py (models/big-lama.pt, ~200MB).
# WITHOUT TORCH OR THE WEIGHTS THE SIDECAR AUTOMATICALLY FALLS BACK TO cv2.inpaint (SOLID-BUBBLE
# QUALITY). NOTE: iopaint IS ARCHIVED AND DOES NOT INSTALL ON PYTHON 3.14 — WE RUN ITS WEIGHTS
# DIRECTLY VIA app/lama.py (SAME TorchScript CONTRACT AS iopaint/model/lama.py).

# TEST ONLY:
#   pip install -r requirements-dev.txt  →  pytest, httpx
