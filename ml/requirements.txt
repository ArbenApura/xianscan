# ML SIDECAR RUNTIME DEPENDENCIES — `pip install -r requirements.txt` INTO THE VENV.
fastapi>=0.115
uvicorn[standard]>=0.30
# AUTO-ACCELERATION: DirectML on Windows (AMD/Intel/NVIDIA GPU + CPU fallback), Standard CPU on Linux/macOS
onnxruntime-directml>=1.19; sys_platform == 'win32'
onnxruntime>=1.19; sys_platform != 'win32'
rapidocr>=3.0
opencv-python-headless>=4.10
numpy>=1.26
pillow>=10.0
python-multipart>=0.0.9
pyclipper>=1.3

# ALL ML MODELS (DETECTION + OCR + LaMa INPAINTING) RUN DIRECTLY VIA onnxruntime (NO PyTorch REQUIRED).
# WEIGHTS DOWNLOAD: python scripts/download_models.py (fetches models/comictextdetector.pt.onnx & models/lama.onnx).

# TEST ONLY:
#   pip install -r requirements-dev.txt  →  pytest, httpx
