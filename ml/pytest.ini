[pytest]
testpaths = tests
pythonpath = .
addopts = -q -n auto
# RAPIDOCR/ONNX DOWNLOADS MUST NEVER HAPPEN INSIDE THE TEST SUITE — THE TESTS USE FAKES ONLY
filterwarnings =
    ignore::DeprecationWarning
